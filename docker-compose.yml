services:
  sync:
    # For local development, build from source
    build: .
    # For production, use released image:
    # image: git.home.alaghband.com/farhad/alexa-photos:latest
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
